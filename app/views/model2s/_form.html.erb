<%= form_with(model: model2, local: true) do |form| %>
  <% if model2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(model2.errors.count, "error") %> prohibited this model2 from being saved:</h2>

      <ul>
        <% model2.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
